<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Drdoc.NavegationPasos">
  <!--Pages can be added as references or inline-->
    <ContentPage>
        <!--<StackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" >
        --><!--place new controls here--><!--
        <Frame MasShaow =" False" BorderColor="Gray" Padding="60,50,60,50">
            <StackLayout Orientation="Vertical">
                <Label HorizontalTextAlignment="Center" FontSize="24" Text="Stop watch Timer 11"/>
                <Label HorizontalTextAlignment="Center" HorizontalOptions="CenterAndExpand" Grid.Column="1" FontSize="30">
                    <Label.FormattedText>
                        <Span Text="{Binding StopWatcHours}" />
                        <Span Text=":" />
                        <Span Text="{Binding StopWatcMinutes}"/>
                        <Span Text=":" />
                        <Span Text="{Binding StopWatcSeconds}"/>
                        </FormattedString>
                    </Label.FormattedText>
                </Label>
            </StackLayout>
        </Frame> >
    </StackLayout>-->
    </ContentPage>
    
</TabbedPage>